import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Thesis_Ionic_App',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
